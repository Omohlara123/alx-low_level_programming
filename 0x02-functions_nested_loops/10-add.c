#include "main.h"
/**
 * add - adds two integers and returns the result
 *
 * @n: number being added
 *
 * @m: number being added
 *
 * Return: Always 0.
 *
 */
int add(int n, int m)
{
	return (n + m);
}
